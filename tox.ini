[tox]
envlist = {py38}

[testenv]
passenv = GOOGLE_APPLICATION_CREDENTIALS TEST_PROJECT_NAME
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/octue
commands =
    coverage run --source octue -m unittest tests.resources.test_datafile.DatafileTestCase -vv
    coverage report --show-missing
    coverage xml
deps = -r requirements-dev.txt
